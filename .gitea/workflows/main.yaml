name: Create JSON
run-name: Parse RSS into JSON
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  generate-json:
    runs-on: ubuntu-latest
    steps:
      - run: |
          apt install -y yq
